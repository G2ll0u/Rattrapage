<header>
    <div class="header-logo">
    {% if controller is defined and controller == 'home' %}
        <h1>Stockflow</h1>
    {% else %}
        <a href="{{ url('home', 'index') }}">
            <img src="{{ BASE_URL }}static/images/logo.png" alt="Logo Stockflow">
        </a>
    {% endif %}
    </div>
    
    <div class="header-controls">
        <div id="user-menu">
            {% if user %}
                <div id="user-icon" class="dropdown">
                    <img src="{{ BASE_URL }}public/images/logo_deco.png" alt="Menu utilisateur" class="user-logo">
                    <div class="dropdown-menu">
                        <p>Bienvenue, {{ user.prenom|e }}</p>
                        <form action="{{ url('utilisateur', 'logout') }}" method="POST">
                            <button type="submit">Déconnexion</button>
                        </form>
                    </div>
                </div>
            {% endif %}
        </div>
        
        <button class="burger-menu" aria-label="Menu de navigation" aria-expanded="false">
            <span></span>
            <span></span>
            <span></span>
        </button>
    </div>

    <nav>
        {% if user %}
        <a href="{{ url('home', 'index') }}">Accueil</a>
        <a href="{{ url('offre', 'index') }}">Produits</a>
        <a href="{{ url('entreprise', 'index') }}">Fournisseurs</a>
        <a href="{{ url('candidature', 'index') }}">Commandes</a>
        <a href="{{ url('stock', 'index') }}">Stocks</a>
        <a href="{{ url('historique', 'index') }}">Historique</a>
        <a href="{{ url('compte', 'index') }}">Mon compte</a>
        <a href="{{ url('contact', 'index') }}">Contact</a>
        <a href="{{ url('dashboard', 'index') }}">Dashboard</a>
        {% endif %}
    </nav>
</header>